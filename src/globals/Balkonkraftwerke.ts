import { GlobalConfig } from 'payload'
import { createPageConfig } from './common/pageFactory'

export const Balkonkraftwerke: GlobalConfig = createPageConfig(
  'balkonkraftwerke-page',
  'Balkonkraftwerke',
)
